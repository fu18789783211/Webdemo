// 全局的setTimeout()方法设置一个定时器，在定时器到期后执行一段代码
// 语法：
// var timeoutId = scope.setTimeout(Function[,dalay,arg1,arg2,...]);
// var timeoutId = scope.setTimeout(Function[,delay]);
// var timeoutId = scope.setTimeout(code[,delay]);
